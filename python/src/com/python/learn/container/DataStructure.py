# 数据类型

# 列表类型
lists = ["1", "2"]

# 字典类型
dicts = {"key": "value"}

# 元组类型
nums = (1, 2, 3, 4)

# 集合类型
arrays = {1, 2, 4, 5}

print("list type:%s,dict type: %s,tuple type: %s,array type: %s" % (type(lists), type(dicts), type(nums), type(arrays)))

